/*
Write a C program that reads an input number and uses if else to check if the input number is even or odd.

NOTE: Output should **exactly** say either "[number] is even" or "[number] is odd" 
*/

#include <stdio.h>
int main()
{

  return 0;
}
